{
  "name": "bloath-cms-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "cd ../web && npm run build && cp -r ../web/dist/* ./public/ && wrangler deploy",
    "build-assets": "cd ../web && npm run build && cp -r ../web/dist/* ./public/"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
